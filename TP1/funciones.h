/** \brief La funcion recibe dos numeros, los suma y devuelve el resultado.
 *
 * \param float primer valor
 * \param float segundo valor
 * \return float resultado de la operacion
 *
 */
float sumar(float numeroUno, float numeroDos);


/** \brief La funcion recibe dos numeros , los resta y devuelve el resultado.
 *
 * \param float primer valor
 * \param float segundo valor
 * \return float resultado de la operacion
 *
 */
float restar(float numeroUno, float numeroDos);


/** \brief La funcion recibe dos numeros, valida si es un 0, los divide y devuelve el resultado.
 *
 * \param float primer valor
 * \param float segundo valor
 * \return float resultado de la operacion
 *
 */
float dividir(float numeroUno, float numeroDos);


/** \brief La funcion recibe dos numeros, los multiplica y devuelve el resultado.
 *
 * \param float primer valor
 * \param float segundo valor
 * \return float resultado de la operacion
 *
 *
 */
float multiplicar(float numeroUno, float numeroDos);


/** \brief La funcion recibe un numero, valida si es un numero negativo, calcula su factorial y devuelve el resultado.
 *
 * \param long int primer valor
 * \return long int resultado de la operacion
 *
 */
long int factorizar(long int numero);


void calculadora();

